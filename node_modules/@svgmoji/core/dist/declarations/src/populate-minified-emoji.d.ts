import type { FlatEmoji, MinifiedEmoji } from './types';
/**
 * Populate the minified emoji into a readable format.
 */
export declare function populateMinifiedEmoji(minified: MinifiedEmoji[]): FlatEmoji[];
